import Colors from './colors';
import * as Scaling from './scaling';
import {moderateScale, scale, verticalScale, width} from './scaling';

export {scale, Colors, Scaling, verticalScale, moderateScale, width};
