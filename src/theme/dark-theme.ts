import {Colors} from '@styles';
import {ColorsThemeTypes} from '@utils/types';

export const darkTheme: ColorsThemeTypes = {
  primary: Colors.primary,
  background: Colors.black,
  text: Colors.white,
  secondary: Colors.secondary,
};
